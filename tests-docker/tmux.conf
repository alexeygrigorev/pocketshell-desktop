# PocketShell standalone local test instance.
#
# The desktop terminal sends wheel events to tmux when the pane asks for mouse
# tracking. With mouse mode enabled, tmux turns the wheel into pane-history
# navigation instead of leaving it to the terminal emulator's own scrollback.
set -g mouse on
set -g history-limit 100000
