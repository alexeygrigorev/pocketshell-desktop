#!/bin/sh
# claude stub: credential-free version string for the bootstrap version probe.
case "$1" in
  --version|-V) echo "claude 0.0.0-stub (pocketshell test fixture)" ;;
  *) echo "claude stub — no real provider credentials in the test image" ;;
esac
